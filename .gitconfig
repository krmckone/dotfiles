[user]
	name = Kaleb McKone
	email = 20476319+krmckone@users.noreply.github.com

[credential]
    helper = "if [[ $(uname) == 'Linux' ]]; then HELPER='store'; else HELPER='osxkeychain'; fi; $HELPER"

[pull]
	rebase = false
# Use a pager for large output, just like other git commands.
[pager]
  difftool = true

[core]
  editor = nvim
[push]
	autoSetupRemote = true
